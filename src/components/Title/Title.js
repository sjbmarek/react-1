import React from "react";
import "./Title.css";

const Title = props => (
	<div className="titleblock p-5">
	<h1 className="title">Clicky Color Dot</h1>
	<h4 className="subtitle pb-3">Click on an image to earn points, but don't click on any more than once!</h4>
	</div>
	);

export default Title;
