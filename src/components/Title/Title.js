import React from "react";
import "./Title.css";

const Title = props => (
	<div className="titleblock pt-5 pb-3">
	<h1 className="title">Korean Alphabet Click</h1>
	<h4 className="subtitle pb-3">Click on a hangul letter to earn points, but don't click on any more than once!</h4>
	</div>
	);

export default Title;
